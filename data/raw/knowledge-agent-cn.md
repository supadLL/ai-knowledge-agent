# 本地知识库说明

AI Knowledge Agent 是一个本地优先的知识库问答工具。

它会读取本地 Markdown 和 TXT 文件，把文档切分成稳定片段，建立可检索索引，并在用户提问后先召回相关片段。

如果燃料池中存在可用的模型账号，系统会把检索到的上下文交给 AI 总结，并要求回答保留来源引用。

如果没有可用模型账号，系统会使用本地抽取式回答，把最相关的证据片段和引用返回给用户。

账号中转只在本软件内部使用，不会修改本机 Codex、OpenAI 或其他全局代理配置。
